import { SakuraMongoDbConnection } from '../core/sakura-mongo-db-connection';
export declare class SakuraApiConfig {
    static dataSources(config: {
        dbConnections?: any[];
    }): SakuraMongoDbConnection;
    config: any;
    constructor();
    dataSources(config?: any): SakuraMongoDbConnection;
    load(path?: string): any;
}
