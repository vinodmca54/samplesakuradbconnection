"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function addDefaultInstanceMethods(target, fn, options) {
    if (options && options.suppressInjection && options.suppressInjection.indexOf(fn.name) > -1) {
        return;
    }
    if (!target.prototype[fn.name]) {
        target.prototype[fn.name] = fn;
    }
}
exports.addDefaultInstanceMethods = addDefaultInstanceMethods;
function addDefaultStaticMethods(target, fn, options) {
    if (options && options.suppressInjection && options.suppressInjection.indexOf(fn.name) > -1) {
        return;
    }
    if (!target[fn.name]) {
        target[fn.name] = fn;
    }
}
exports.addDefaultStaticMethods = addDefaultStaticMethods;
//# sourceMappingURL=defaultMethodHelpers.js.map