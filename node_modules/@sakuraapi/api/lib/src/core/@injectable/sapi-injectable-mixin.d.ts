import { Constructor } from '../helpers/constructor-type';
import { SakuraApi } from '../sakura-api';
export declare function SapiInjectableMixin<T extends Constructor<{}>>(base?: T): {
    new (...args: any[]): {
        sapi: SakuraApi;
        sapiConfig: any;
    };
    sapi: SakuraApi;
    sapiConfig: any;
} & T;
