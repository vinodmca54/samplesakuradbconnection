/// <reference types="express" />
import { Handler } from 'express';
import { IAuthenticatorConstructor } from '../plugins';
export interface IRoutableMethodOptions {
    authenticator?: IAuthenticatorConstructor[] | IAuthenticatorConstructor;
    path?: string;
    method?: string;
    blackList?: boolean;
    before?: [Handler] | Handler;
    after?: [Handler] | Handler;
}
export declare function Route(options?: IRoutableMethodOptions): (target: any, key: string, value: TypedPropertyDescriptor<any>) => {
    value: (...args: any[]) => any;
};
