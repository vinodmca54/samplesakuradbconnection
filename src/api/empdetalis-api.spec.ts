import {testSapi} from '../../spec/helpers/sakura-api';
import {EmpdetalisApi} from './empdetalis-api';

describe('EmpdetalisApi tests', () => {
  const sapi = testSapi({
    routables: [EmpdetalisApi]
  });

  it('EmpdetalisApi', () => {
    pending('not implemented');
  });
});
